export enum GitIssueState {
  OPENED = 'opened',
  CLOSED = 'closed',
}

export enum GitMilestoneState {
  ACTIVE = 'active',
  EXPIRED = 'expired',
}

export enum GitUserState {
  ACTIVE = 'active',
  DE_ACTIVE = 'deactive',
}
